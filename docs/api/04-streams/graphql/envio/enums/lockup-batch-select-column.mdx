---
id: lockup-batch-select-column
title: LockupBatch_select_column
---

<head>
  <meta content="noindex" name="robots" />
  <meta charset="utf-8" />
</head>

export const Bullet = () => (
  <>
    <span style={{ fontWeight: "normal", fontSize: ".5em", color: "var(--ifm-color-secondary-darkest)" }}>
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({ dataOpen, dataClose, children, startOpen = false }) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{ border: "none", boxShadow: "none", background: "var(--ifm-background-color)" }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

select columns of table "LockupBatch"

```graphql
enum LockupBatch_select_column {
  batcher_id
  hash
  id
  position
  size
  timestamp
}
```

### Values

#### [<code style={{ fontWeight: 'normal' }}>LockupBatch_select_column.<b>batcher_id</b></code>](#batcher-id) \{#batcher-id\}

column name

#### [<code style={{ fontWeight: 'normal' }}>LockupBatch_select_column.<b>hash</b></code>](#hash) \{#hash\}

column name

#### [<code style={{ fontWeight: 'normal' }}>LockupBatch_select_column.<b>id</b></code>](#id) \{#id\}

column name

#### [<code style={{ fontWeight: 'normal' }}>LockupBatch_select_column.<b>position</b></code>](#position) \{#position\}

column name

#### [<code style={{ fontWeight: 'normal' }}>LockupBatch_select_column.<b>size</b></code>](#size) \{#size\}

column name

#### [<code style={{ fontWeight: 'normal' }}>LockupBatch_select_column.<b>timestamp</b></code>](#timestamp) \{#timestamp\}

column name
