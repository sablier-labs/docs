---
id: sponsor-order-by
title: Sponsor_orderBy
---

<head>
  <meta content="noindex" name="robots" />
  <meta charset="utf-8" />
</head>

export const Bullet = () => (
  <>
    <span style={{ fontWeight: "normal", fontSize: ".5em", color: "var(--ifm-color-secondary-darkest)" }}>
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({ dataOpen, dataClose, children, startOpen = false }) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{ border: "none", boxShadow: "none", background: "var(--ifm-background-color)" }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

No description

```graphql
enum Sponsor_orderBy {
  id
  address
  chainId
  sponsorshipCount
  sponsorships
  totalAmount
  totalAmountDisplay
}
```

### Values

#### [<code style={{ fontWeight: 'normal' }}>Sponsor_orderBy.<b>id</b></code>](#id) \{#id\}

#### [<code style={{ fontWeight: 'normal' }}>Sponsor_orderBy.<b>address</b></code>](#address) \{#address\}

#### [<code style={{ fontWeight: 'normal' }}>Sponsor_orderBy.<b>chainId</b></code>](#chain-id) \{#chain-id\}

#### [<code style={{ fontWeight: 'normal' }}>Sponsor_orderBy.<b>sponsorshipCount</b></code>](#sponsorship-count) \{#sponsorship-count\}

#### [<code style={{ fontWeight: 'normal' }}>Sponsor_orderBy.<b>sponsorships</b></code>](#sponsorships) \{#sponsorships\}

#### [<code style={{ fontWeight: 'normal' }}>Sponsor_orderBy.<b>totalAmount</b></code>](#total-amount) \{#total-amount\}

#### [<code style={{ fontWeight: 'normal' }}>Sponsor_orderBy.<b>totalAmountDisplay</b></code>](#total-amount-display) \{#total-amount-display\}
