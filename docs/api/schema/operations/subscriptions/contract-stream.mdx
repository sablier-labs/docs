---
id: contract-stream
title: Contract_stream
---

export const Bullet = () => (
  <>
    <span style={{ fontWeight: "normal", fontSize: ".5em", color: "var(--ifm-color-secondary-darkest)" }}>
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({ dataOpen, dataClose, children, startOpen = false }) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{ border: "none", boxShadow: "none", background: "var(--ifm-background-color)" }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

fetch data from the table in a streaming manner: "Contract"

```graphql
Contract_stream(
  batch_size: Int!
  cursor: [Contract_stream_cursor_input]!
  where: Contract_bool_exp
): [Contract!]!
```

### Arguments

#### [<code style={{ fontWeight: 'normal' }}>Contract_stream.<b>batch_size</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>Int!</b></code>](/types/scalars/int.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="scalar"/>

maximum number of rows returned in a single batch

#### [<code style={{ fontWeight: 'normal' }}>Contract_stream.<b>cursor</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>[Contract_stream_cursor_input]!</b></code>](/types/inputs/contract-stream-cursor-input.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="input"/>

cursor to stream the results returned by the query

#### [<code style={{ fontWeight: 'normal' }}>Contract_stream.<b>where</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>Contract_bool_exp</b></code>](/types/inputs/contract-bool-exp.mdx) <Badge class="badge badge--secondary " text="input"/>

filter the rows returned

### Type

#### [<code style={{ fontWeight: 'normal' }}><b>Contract</b></code>](/types/objects/contract.mdx) <Badge class="badge badge--secondary " text="object"/>

columns and relationships of "Contract"
