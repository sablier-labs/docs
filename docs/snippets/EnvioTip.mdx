:::tip Chains: Individual vs. Aggregated

+While subgraphs are chain-tethered (one subgraph endpoint per chain), Envio indexes all chains within a single
+deployment. Data from all the chains where Sablier is deployed can be queried from a single API.

:::
