{/* eslint-disable react/prop-types */}

import Link from "@docusaurus/Link";
import Admonition from "@theme/Admonition";

export default function DeprecatedSubgraphs({ previous }) {
  return (
    <Admonition type="warning">

      In February 2025, we deprecated the old series of `sablier-v2-*` subgraphs endpoints. They will remain available, but
      they will not track the latest versions of the Sablier Protocol. If you need to use the deprecated endpoints, head over
      to the <Link href={previous}>Previous Endpoints</Link> section.

    </Admonition>

)}
